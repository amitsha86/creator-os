# Copy to .env.local and add your key to enable live AI generation.
# Without a key, the app runs fully with high-quality fallback responses.
ANTHROPIC_API_KEY=
# Optional: override the model (defaults to claude-sonnet-4-6)
ANTHROPIC_MODEL=claude-sonnet-4-6
